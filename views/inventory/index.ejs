<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
    }
    h1 {
      text-align: center;
      margin-bottom: 30px;
    }
    .inventory-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;
    }
    .inventory-card {
      border: 1px solid #ccc;
      border-radius: 8px;
      padding: 15px;
      width: 250px;
      text-align: center;
      box-shadow: 2px 2px 6px rgba(0,0,0,0.1);
      background-color: #fafafa;
    }
    .inventory-card img {
      max-width: 100%;
      height: auto;
      border-radius: 4px;
      margin-bottom: 10px;
    }
    .inventory-card h3 {
      margin: 10px 0;
      font-size: 1.2em;
    }
    .inventory-card p {
      margin: 5px 0;
      font-size: 0.95em;
    }
    .no-items {
      text-align: center;
      font-style: italic;
      color: #666;
    }
  </style>
</head>
<body>
  <h1><%= title %></h1>
  <p style="text-align:center;">This is the inventory homepage.</p>

  <div class="inventory-grid">
    <% if (items && items.length > 0) { %>
      <% items.forEach(item => { %>
        <div class="inventory-card">
          <a href="/inv/<%= item.inv_id %>">
            <h3><%= item.inv_make %> <%= item.inv_model %> (<%= item.inv_year %>)</h3>
          </a>
          <img src="<%= item.inv_image %>" alt="<%= item.inv_make %> <%= item.inv_model %>">
          <p>Color: <%= item.inv_color %></p>
          <p>Price: $<%= item.inv_price %></p>
          <p>Miles: <%= item.inv_miles %></p>
        </div>
      <% }) %>
    <% } else { %>
      <p class="no-items">No items found in inventory.</p>
    <% } %>
  </div>
</body>
</html>

